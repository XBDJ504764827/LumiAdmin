DATABASE_URL=postgres://user:pass@localhost:5432/manger
BIND_ADDR=0.0.0.0:3001
DEV_USERNAME=dev
DEV_PASSWORD=change-me
HTTP_TIMEOUT_SECS=300
REQUEST_TIMEOUT_SECS=300
MAP_SYNC_SCAN_INTERVAL_SECS=60

# Map sync remote agent config. Set these on each game/download server
# that runs `cargo run --bin map_sync_agent` or the compiled agent binary.
MAP_SYNC_AGENT_BASE_URL=https://admin.example.com
MAP_SYNC_AGENT_TOKEN=replace-with-map-sync-agent-token
MAP_SYNC_AGENT_MAP_DIR=/path/to/csgo/maps
MAP_SYNC_AGENT_MAPLIST_PATH=/path/to/csgo/maplist.txt
MAP_SYNC_AGENT_MAPCYCLE_PATH=/path/to/csgo/mapcycle.txt
MAP_SYNC_AGENT_SYNC_MAP_POOL_FILES=true
MAP_SYNC_AGENT_POLL_INTERVAL_SECS=300
MAP_SYNC_AGENT_TASK_LIMIT=20
MAP_SYNC_AGENT_RUN_ONCE=false

# Request body limit should be higher than the largest appeal file because
# multipart uploads include small boundary/header overhead.
APPEAL_FILE_MAX_SIZE_MB=100
MAX_REQUEST_BODY_BYTES=115343360

# Cloudflare R2 / S3-compatible storage for ban appeal evidence files.
# Leave these empty to disable file upload without affecting other features.
R2_ENDPOINT=https://example-account-id.r2.cloudflarestorage.com
R2_BUCKET=example-bucket
R2_ACCESS_KEY_ID=replace-me
R2_SECRET_ACCESS_KEY=replace-me
R2_CUSTOM_DOMAIN=https://files.example.com
R2_TOKEN_VALUE=
